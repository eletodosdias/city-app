/*interface User {

    firstName: string
    lastName: string
}
*/
    //default:nell'import non servono parentesi graffe
export default class City{
    constructor(public id: number, public name: string, public region: string){}
}